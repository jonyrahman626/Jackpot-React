import React from 'react';
import Error from '../components/Error/Error';

const Promotion = () => {
    return (
        <div>
            <Error />
        </div>
    );
}

export default Promotion;
